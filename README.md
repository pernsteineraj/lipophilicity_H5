# Installation
1. Clone the repository: `git clone <YOUR_REPO_URL>`
2. Navigate to the directory: `cd lipophilicity-package`
3. Create the Conda environment: `conda env create -f environment.yml`
4. Activate the environment: `conda activate lipo_env`
